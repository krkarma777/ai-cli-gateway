version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - bodyclose
    - errcheck
    - errorlint
    - exhaustive
    - gosec
    - nilerr
    - noctx
    - revive
    - staticcheck
    - unconvert
formatters:
  enable:
    - gofmt
    - goimports
