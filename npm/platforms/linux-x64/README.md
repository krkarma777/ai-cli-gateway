# ai-cli-gateway-linux-x64

This package contains the native AI CLI Gateway binary for the `linux-x64` target (`GOOS=linux`, `GOARCH=amd64`). It is installed through `ai-cli-gateway` and must not be installed or used directly.
