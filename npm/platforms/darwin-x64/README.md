# ai-cli-gateway-darwin-x64

This package contains the native AI CLI Gateway binary for the `darwin-x64` target (`GOOS=darwin`, `GOARCH=amd64`). It is installed through `ai-cli-gateway` and must not be installed or used directly.
