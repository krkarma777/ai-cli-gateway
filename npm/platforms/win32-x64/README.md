# ai-cli-gateway-win32-x64

This package contains the native AI CLI Gateway binary for the `win32-x64` target (`GOOS=windows`, `GOARCH=amd64`). It is installed through `ai-cli-gateway` and must not be installed or used directly.
