# ai-cli-gateway-darwin-arm64

This package contains the native AI CLI Gateway binary for the `darwin-arm64` target (`GOOS=darwin`, `GOARCH=arm64`). It is installed through `ai-cli-gateway` and must not be installed or used directly.
