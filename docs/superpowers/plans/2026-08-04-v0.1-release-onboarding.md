# v0.1 Release and SDK Onboarding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

**Goal:** Publish AI CLI Gateway v0.1.0 as a verified five-platform GitHub Release and prove the existing non-streaming Responses API-compatible subset through the official OpenAI Python and JavaScript SDKs.

**Architecture:** A small internal Go package validates fixed release inputs, writes deterministic archives, finalizes a collection SPDX document, and emits checksums. A separate tag-only workflow calls the existing read-only CI, packages and attests assets without publication authority, verifies them in a read-only job, and grants contents write only to a final draft-first publisher. A Go SDK-contract runner launches the real gateway with a deterministic fake Codex CLI; a thin shell entrypoint and checked-in official SDK examples exercise the public HTTP boundary.

**Tech Stack:** Go 1.26.5 standard library, Go debug/buildinfo, GitHub Actions, Syft v1.50.0 through anchore/sbom-action v0.24.0, actions/attest v4.2.1, OpenAI Python 2.53.0 on Python 3.12, OpenAI JavaScript 6.48.0 on Node 24.

## Global Constraints

- The controlling specification is docs/superpowers/specs/2026-08-04-v0.1-release-onboarding-design.md.
- Do not add an HTTP endpoint, streaming, tool calls, sessions, storage, a web UI, or a database.
- Keep the product description “Responses API-compatible subset”; never claim complete OpenAI API or SDK compatibility.
- Release tags must match exactly ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$.
- Release targets are exactly linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, and windows/amd64.
- Build with Go 1.26.5, CGO_ENABLED=0, -trimpath, -buildvcs=false, -mod=readonly, and linker flags -s -w.
- Release outputs, SDK environments, node_modules, generated configs, logs, and binaries must remain outside the repository.
- Releasepack executes on Linux and macOS; Windows is an archive and compile target only. Its output root is an exact-mode-0700, effective-UID-owned, unshared transaction workspace beneath validated non-replaceable ancestor authority. Another process with the same OS identity must not mutate it while a releasepack subcommand runs, and such concurrent mutation is outside the v0.1 threat model.
- Never commit a provider credential, GitHub token, real authentication file, prompt, model output, or private filesystem path.
- Provider and gateway processes always receive argv arrays and stdin; never construct a shell command string from request data.
- SDK retries are disabled. The checked-in clients use a bounded 1..300-second timeout with a 300-second user default; the deterministic harness always sets it to exactly five seconds.
- The gateway binds only to 127.0.0.1 in all examples and tests.
- Keep Makefile’s exact eight public targets unchanged.
- Each implementation task must show a semantic RED before production code, pass its focused tests, receive specification and quality review, and end in its own commit.

## File Structure

Release packaging:

- internal/releasepack/contract.go — fixed targets, archive contents, options, and assets.
- internal/releasepack/errors.go — closed non-sensitive error categories.
- internal/releasepack/validate.go — tag, timestamp, shared root, source, and staging validation.
- internal/releasepack/root_unix.go — Linux/macOS output ownership, mode, and ancestor-authority validation.
- internal/releasepack/root_unsupported.go — fail-closed releasepack-host guard outside Linux/macOS.
- internal/releasepack/archive.go — sorted archive-entry planning, exclusive output lifecycle, and no-clobber publication.
- internal/releasepack/targz.go — deterministic USTAR plus gzip writer.
- internal/releasepack/zip.go — deterministic ZIP writer.
- internal/releasepack/sbom.go — Syft SPDX parsing, build-info comparison, five binary records, and path hygiene.
- internal/releasepack/checksum.go — exact six-record SHA256SUMS writer.
- internal/releasepack/cmd/releasepack/main.go — closed archives, sbom, and checksums subcommands.
- internal/releasepack/*_test.go — real-reader, raw-header, SPDX, checksum, and command tests.

SDK contract:

- internal/testcli/fake.go — codex-ready probe and final-output mode.
- internal/testcli/cmd/fake-codex-cli/main.go — dedicated Codex fixture executable.
- examples/config/codex.example.toml — minimal single-provider configuration.
- examples/openai-sdk/python/main.py and dependency files — official Python client contract.
- examples/openai-sdk/javascript/main.mjs and package files — official JavaScript client contract.
- internal/sdkcontract/runner.go — retry, readiness, client execution, redaction, and cleanup state machine.
- internal/sdkcontract/system.go — real filesystem, build, HTTP, and child-process dependencies.
- internal/sdkcontract/child_unix.go and child_windows.go — process lifecycle boundary.
- internal/sdkcontract/cmd/sdk-contract/main.go — fixed command-line wrapper.
- scripts/sdk-contract.sh — repository-root guard and argv-only Go runner invocation.

Automation and docs:

- .github/workflows/ci.yml — reusable CI trigger plus Linux SDK contract job.
- .github/workflows/release.yml — verify, package, asset-verification, and publish jobs.
- internal/securitytest/repository_test.go — exact workflow, example, README, and secret-scanner contracts.
- README.md — release-first POSIX and PowerShell Quick Start, verification, configuration, curl, and SDK usage.

---

### Task 1: Admit Only GitHub’s Ephemeral Workflow Token Expression

**Files:**
- Modify: internal/securitytest/repository_test.go
- Test: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes: existing isAllowedPlaceholder(value string) bool.
- Produces: the exact expression ${{ github.token }} is allowed; every other GitHub or secrets expression remains rejected.

- [ ] **Step 1: Write the failing boundary test**

Add this table test beside the existing placeholder/scanner tests:

    func TestAllowedPlaceholderAcceptsOnlyGitHubTokenContext(t *testing.T) {
        tests := []struct {
            value string
            want  bool
        }{
            {value: "${{ github.token }}", want: true},
            {value: "${{ github.actor }}", want: false},
            {value: "${{ secrets.GITHUB_TOKEN }}", want: false},
            {value: "${{ github.token }}suffix", want: false},
            {value: "${{github.token}}", want: false},
        }
        for _, test := range tests {
            if got := isAllowedPlaceholder(test.value); got != test.want {
                t.Errorf("isAllowedPlaceholder(%q) = %t, want %t", test.value, got, test.want)
            }
        }
    }

- [ ] **Step 2: Run the focused RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run TestAllowedPlaceholderAcceptsOnlyGitHubTokenContext -count=1

Expected: FAIL for ${{ github.token }} because the current generic environment placeholder grammar rejects spaces and nested braces.

- [ ] **Step 3: Add the exact allowlist before the generic environment rule**

Insert this exact branch after quote removal and before the existing ${NAME} parser:

    if trimmed == "${{ github.token }}" {
        return true
    }

Do not widen the grammar to github.*, secrets.*, arbitrary whitespace, or arbitrary workflow expressions.

- [ ] **Step 4: Verify focused and full repository hygiene**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run 'TestAllowedPlaceholderAcceptsOnlyGitHubTokenContext|TestRepositoryHygiene' -count=1

Expected: PASS.

- [ ] **Step 5: Commit**

    git add internal/securitytest/repository_test.go
    git commit -m "test: admit GitHub workflow token context"

### Task 2: Validate the Closed Release-Packaging Input Contract

**Files:**
- Create: internal/releasepack/contract.go
- Create: internal/releasepack/errors.go
- Create: internal/releasepack/validate.go
- Create: internal/releasepack/validate_test.go

**Interfaces:**
- Consumes: fixed repository module github.com/krkarma777/ai-cli-gateway and public files already present at repository root.
- Produces:

    type ArchiveOptions struct {
        RepositoryRoot string
        StagingRoot    string
        OutputRoot     string
        Tag            string
        SourceTime     time.Time
    }

    type Asset struct {
        Name string
        Path string
    }

    func ErrorCategory(error) string

Internal validateRootSet(repositoryRoot, stagingRoot, outputRoot string, allowAbsentOutput bool) error validates absolute, clean, non-symlink, pairwise-disjoint roots without imposing output membership. validateRepositoryAndStaging(repositoryRoot, stagingRoot string) (sourceSet, []stagedBinary, error) validates the reusable read-only inputs. validateTag(tag string) (version string, err error) and validateSourceTime(time.Time) (time.Time, error) own the reusable primitives; validateTagAndSourceTime composes them for archives/SBOM. newArchivePlan(ArchiveOptions) (archivePlan, error) adds the empty-output contract for Task 3. Task 3 later extends validateRootSet with the approved host and output-authority boundary before Task 4 reuses it with nonempty output membership rules.

- [ ] **Step 1: Write table-driven RED tests for exact accepted input**

Build a private fixture with the expected go.mod declaration, README.md, LICENSE, THIRD_PARTY_NOTICES.md, config.example.toml, examples/config/codex.example.toml, all six examples/openai-sdk Python/JavaScript source and lock/manifest files from Task 7, deploy/systemd/ai-cli-gateway.service, and these exact staging children:

    linux_amd64/ai-cli-gateway
    linux_arm64/ai-cli-gateway
    darwin_amd64/ai-cli-gateway
    darwin_arm64/ai-cli-gateway
    windows_amd64/ai-cli-gateway.exe

The success test must assert five targets in the listed order and version 0.1.0 for tag v0.1.0. Rejection cases must cover:

- v0.1, v01.0.0, v0.1.0-rc.1, v0.1.0+build, leading/trailing whitespace, and shell metacharacters;
- local time, nonzero nanoseconds, 1979-12-31T23:59:59Z, and a time after Unix uint32 maximum;
- relative roots, symlink components, overlapping roots, wrong go.mod module, missing public source, non-regular public source, and a symlinked descendant directory such as examples or examples/config;
- one missing target, one extra target, a target directory with an extra file, wrong executable suffix, a symlinked target directory, and a staged executable symlink;
- a nonempty output root and an output symlink.

Define this complete package-wide ErrorCategory set in Task 2:

    invalid_tag
    invalid_usage
    unsafe_path
    missing_input
    archive_failure
    sbom_failure
    checksum_failure
    internal_error

Task 2 rejection cases exercise only invalid_tag, unsafe_path, and missing_input. Require ErrorCategory(nil) == "" and an unknown foreign error to map to internal_error. Later tasks exercise their applicable failure categories without adding new values.

- [ ] **Step 2: Run the package RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack -run 'TestNewArchivePlan|TestErrorCategory' -count=1

Expected: compile failure because ArchiveOptions, ErrorCategory, and newArchivePlan do not exist.

- [ ] **Step 3: Implement fixed targets and categories**

Define the immutable target contract:

    type archiveFormat uint8

    const (
        formatTarGzip archiveFormat = iota + 1
        formatZIP
    )

    type target struct {
        GOOS           string
        GOARCH         string
        Directory      string
        Executable     string
        Format         archiveFormat
        IncludeSystemd bool
    }

    var releaseTargets = [...]target{
        {GOOS: "linux", GOARCH: "amd64", Directory: "linux_amd64", Executable: "ai-cli-gateway", Format: formatTarGzip, IncludeSystemd: true},
        {GOOS: "linux", GOARCH: "arm64", Directory: "linux_arm64", Executable: "ai-cli-gateway", Format: formatTarGzip, IncludeSystemd: true},
        {GOOS: "darwin", GOARCH: "amd64", Directory: "darwin_amd64", Executable: "ai-cli-gateway", Format: formatTarGzip},
        {GOOS: "darwin", GOARCH: "arm64", Directory: "darwin_arm64", Executable: "ai-cli-gateway", Format: formatTarGzip},
        {GOOS: "windows", GOARCH: "amd64", Directory: "windows_amd64", Executable: "ai-cli-gateway.exe", Format: formatZIP},
    }

Use a private categorized error type whose Error method returns only its category. ErrorCategory must return an empty string for nil, internal_error for an unknown foreign error, and never include an underlying path or OS message.

- [ ] **Step 4: Implement path, tag, timestamp, and membership validation**

Use a compiled exact SemVer expression and validate SourceTime with:

    sourceTime.Location() == time.UTC
    sourceTime.Nanosecond() == 0
    sourceTime.Unix() >= time.Date(1980, 1, 1, 0, 0, 0, 0, time.UTC).Unix()
    sourceTime.Unix() <= int64(math.MaxUint32)

For every accepted root:

- require filepath.IsAbs(path) and filepath.Clean(path) == path;
- Lstat every existing component and reject symlinks;
- require repository and staging roots to be existing real directories;
- allow output to be absent or an empty real directory whose parent chain is non-symlink;
- use filepath.Rel in both directions to reject equal or nested roots;
- enumerate directories with os.ReadDir and compare exact names before opening content;
- walk every fixed descendant path component from its already validated root with Lstat, rejecting a symlink at an intermediate directory or leaf;
- require each expected descendant directory to be a real directory and each fixed leaf to satisfy Mode().IsRegular().

Return an archivePlan containing copied target values, version without v, validated source paths, and no caller-owned slices or maps.

- [ ] **Step 5: Run the focused GREEN and platform compile**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack -run 'TestNewArchivePlan|TestErrorCategory' -count=1
    GOOS=windows GOARCH=amd64 go test -c -o /private/tmp/releasepack-windows.test ./internal/releasepack

Expected: both commands PASS; no output file appears inside the repository.

- [ ] **Step 6: Commit**

    git add internal/releasepack/contract.go internal/releasepack/errors.go internal/releasepack/validate.go internal/releasepack/validate_test.go
    git commit -m "feat: validate release packaging inputs"

### Task 3: Write Byte-Deterministic tar.gz and ZIP Archives

**Files:**
- Modify: internal/releasepack/validate.go
- Modify: internal/releasepack/validate_test.go
- Modify: internal/releasepack/errors.go
- Create: internal/releasepack/archive.go
- Create: internal/releasepack/targz.go
- Create: internal/releasepack/zip.go
- Create: internal/releasepack/root_unix.go
- Create: internal/releasepack/root_unsupported.go
- Create: internal/releasepack/host_unsupported_test.go
- Create: internal/releasepack/archive_test.go
- Create: internal/releasepack/zip_test.go

**Interfaces:**
- Consumes: ArchiveOptions, Asset, target, archivePlan, and newArchivePlan from Task 2.
- Produces:

    func WriteArchives(ArchiveOptions) ([]Asset, error)

Task 3 extends validateRootSet before any root inspection with the private boundary used by all later releasepack operations. The platform files provide:

    func validateReleasepackHost() error
    func validateOutputAuthority(string, fs.FileInfo) error

root_unix.go begins with `//go:build linux || darwin`; validateReleasepackHost returns nil there. A nil FileInfo means the leaf is absent and only its existing parent/ancestors are validated. A non-nil FileInfo additionally requires a real effective-UID-owned directory with permission bits exactly 0700 and no setuid, setgid, or sticky bits. root_unsupported.go begins with `//go:build !linux && !darwin`; validateReleasepackHost returns invalid_usage without inspecting caller data. validateRootSet calls that guard before inspecting any path.

Use `var releasepackEffectiveUID = os.Geteuid` as the single narrow Unix policy seam. Production compares its uint32 value with `syscall.Stat_t.Uid`; foreign-owner tests replace the function, restore it with t.Cleanup, and never run in parallel. No filesystem operation, mode result, or error text is injected through this seam.

validate_test.go, archive_test.go, and zip_test.go begin with `//go:build linux || darwin`. host_unsupported_test.go begins with `//go:build !linux && !darwin` and calls WriteArchives with valid-shaped platform-absolute but nonexistent paths, proving it returns only invalid_usage before filesystem inspection. Do not use a TestMain that silently skips all tests. Production and tests must still compile on Windows.

Under the Global Constraints' exclusive-output precondition, it returns exactly five assets in target order. Validation failures propagate the existing closed category unchanged, including invalid_usage on an unsupported host; archive_failure applies only to archive planning, serialization, publication, or cleanup after successful input validation. On such an operational failure it returns no assets, then best-effort removes only paths still proven owned. A persistent filesystem cleanup failure taints the entire private output root, which the caller must discard; no later releasepack or publication step may consume it. Portable pathname deletion cannot atomically compare file identity and unlink, so identity checks before cleanup are defense in depth and must not be described as protection from an excluded same-credential concurrent racer.

Use the Task 2 archive_failure category. Archive tests must force create, copy, close, chmod, link-publication, temporary-unlink, cleanup-unlink, and output-root-removal failures through narrowly injected filesystem hooks without exposing an OS error or path. Recoverable one-shot failures must clean every owned path. Persistent cleanup failures must return no assets, retain only the paths whose deletion was refused inside the tainted private root, and never report success.

- [ ] **Step 1: Write real-reader archive RED tests**

For tar.gz, use gzip.NewReader and tar.NewReader. For ZIP, use zip.NewReader. Assert:

- exact top-level directory matching each archive basename;
- common executable/docs/config entries plus all six official SDK source/manifest/lock files in lexical order;
- deploy/systemd/ai-cli-gateway.service only in Linux archives;
- directory 0755, Unix executable 0755, Windows executable/public files 0644;
- tar USTAR, uid/gid zero, empty uname/gname/linkname, exact UTC modtime;
- gzip empty Name, Comment, and Extra with fixed OS byte;
- ZIP empty comments and exact modtime;
- no absolute entry, dot-dot component, backslash entry, duplicate entry, symlink, device, or hard link.
- every regular entry's payload bytes exactly equal the corresponding fixed repository or staged-binary fixture source.

Package the same fixture into two separate empty output roots and compare every archive byte-for-byte.

Assert an absent output root is created, descriptor-chmodded, and reverified with exact mode 0700 even while a scoped Unix test temporarily installs umask 0777; restore the process-global umask before any assertion and never run that test in parallel. Assert an existing empty effective-UID-owned mode-0700 root is accepted, while mode-0755, a foreign synthetic owner through the Unix policy seam, and an unsafe writable non-sticky ancestor fail before publishing any asset. Add a post-link replacement hook test: after the real no-clobber link succeeds, replace the final name before the hook returns success; WriteArchives must fail without adopting or deleting the replacement. The unsupported-host test must execute and assert the fail-closed guard; the explicit Windows compile command remains mandatory.

- [ ] **Step 2: Write raw ZIP local/central-header RED tests**

Parse signatures 0x04034b50, 0x02014b50, and 0x06054b50 from the generated bytes. For every entry assert:

- central and local names match;
- comment length is zero;
- Extra contains exactly one 0x5455 field;
- that field has flags byte 1 and the little-endian uint32 normalized Unix timestamp;
- no second extra field exists.

Use an odd-second source time such as 2026-08-04T06:29:53Z so a DOS-only timestamp implementation cannot pass.

- [ ] **Step 3: Run semantic RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack -run 'TestWriteArchives|TestZIPHeaders' -count=1

Expected: compile failure because WriteArchives is absent.

- [ ] **Step 4: Implement sorted entry planning and exclusive-root cleanup**

Represent entries without caller-controlled names:

    type archiveEntry struct {
        Name       string
        SourcePath string
        Mode       fs.FileMode
        Directory  bool
    }

Generate directory entries explicitly, sort by Name, and reject a duplicate after sorting. Move the exact mode/owner/ancestor checks into validateRootSet so archive, SBOM, and checksum calls share them. For an absent output root, create it with mode 0700, open it, descriptor-chmod it to 0700 to defeat umask filtering, then re-stat the descriptor and pathname to require directory type, matching identity, effective-UID ownership, exact 0700 permission bits, and no setuid/setgid/sticky bits before writing. Accept an existing empty output root only after the same shared validation. Create archive files through os.CreateTemp inside that root. Close and chmod each completed temporary file to 0644, publish it without clobber through os.Link(temp, fixedFinal), and verify the final Lstat identity matches the completed temporary file before tracking the final as owned. A failed post-link identity check must not adopt or remove the unexpected final. Remove the temporary name only after that verification. Never use os.Rename where a pre-existing final name could be overwritten.

On any failure, close open handles and best-effort remove only tracked temporary/final paths whose immediately checked identity still matches the owned identity; a hook that inserts the final name immediately before publication must fail closed without changing that file. Cleanup itself returns success/failure instead of being discarded. A recoverable one-shot remove failure must be removed by the rollback pass. A persistent remove failure returns archive_failure with no assets and leaves the private root tainted for whole-root disposal by the caller; the workflow and dry-run command sequence must stop before SBOM or any later step. Do not claim this check-then-remove sequence is atomic against a same-credential concurrent racer: that racer is excluded by the Global Constraints. Identity checking remains defense in depth against replacements observable before cleanup.

- [ ] **Step 5: Implement deterministic USTAR and gzip**

Set gzip Header fields explicitly:

    writer.Name = ""
    writer.Comment = ""
    writer.Extra = nil
    writer.ModTime = plan.SourceTime
    writer.OS = 255

For each tar.Header set Format tar.FormatUSTAR, Typeflag explicitly to tar.TypeDir or tar.TypeReg, numeric modes, zero ownership, empty identity/link fields, exact size, and exact modtime. Directory names end in slash. After opening a regular file, require os.SameFile between the validated Lstat identity and the handle's Stat result, stream with io.CopyN for the validated size, probe one additional byte for EOF, and re-stat the handle to reject shrink/growth or identity changes during packaging.

- [ ] **Step 6: Implement deterministic ZIP**

Create zip.FileHeader values with forward-slash names, Store for directories, Deflate for files, and an empty comment. Call FileHeader.SetModTime(plan.SourceTime), call SetMode with fs.ModeDir|0755 for directories and the fixed regular-file mode otherwise, and leave Extra empty before CreateHeader so Go writes only its deterministic 0x5455 field. Stream the validated size, probe one additional byte for EOF, and re-stat the open handle as in tar.

- [ ] **Step 7: Run GREEN, determinism, and full releasepack tests**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack -run 'TestWriteArchives|TestZIPHeaders' -count=1
    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack -count=10

Expected: PASS on all ten runs with byte-identical archive assertions.

On Windows, compile the package and tests but do not execute the Linux/macOS releasepack behavioral contract:

    GOOS=windows GOARCH=amd64 GOCACHE=/private/tmp/spawngate-go-build go test -c -o /private/tmp/releasepack-task3-windows.test ./internal/releasepack

Expected: PASS; no output file appears inside the repository.

- [ ] **Step 8: Commit**

    git add internal/releasepack/validate.go internal/releasepack/validate_test.go internal/releasepack/errors.go internal/releasepack/archive.go internal/releasepack/targz.go internal/releasepack/zip.go internal/releasepack/root_unix.go internal/releasepack/root_unsupported.go internal/releasepack/host_unsupported_test.go internal/releasepack/archive_test.go internal/releasepack/zip_test.go
    git commit -m "feat: package deterministic release archives"

### Task 4: Finalize the Collection SPDX, Checksums, and Releasepack CLI

**Files:**
- Modify: internal/releasepack/errors.go
- Modify: internal/releasepack/validate.go
- Create: internal/releasepack/sbom.go
- Create: internal/releasepack/sbom_test.go
- Create: internal/releasepack/testdata/syft-1.50.0-five-binaries.spdx.json
- Create: internal/releasepack/checksum.go
- Create: internal/releasepack/checksum_test.go
- Modify: internal/releasepack/host_unsupported_test.go
- Create: internal/releasepack/cmd/releasepack/main.go
- Create: internal/releasepack/cmd/releasepack/main_test.go
- Create: internal/releasepack/cmd/releasepack/main_unsupported_test.go

**Interfaces:**
- Consumes: fixed target and validation contract from Tasks 2–3, internal/safejson, and debug/buildinfo.
- Produces:

    type SBOMOptions struct {
        RepositoryRoot string
        StagingRoot    string
        OutputRoot     string
        RawPath        string
        Tag            string
        SourceTime     time.Time
    }

    type ChecksumOptions struct {
        RepositoryRoot string
        StagingRoot    string
        OutputRoot     string
        Tag            string
    }

    func WriteSBOM(SBOMOptions) (Asset, error)
    func WriteChecksums(ChecksumOptions) (Asset, error)

The CLI exposes exactly archives, sbom, and checksums and prints only releasepack: CATEGORY on failure.

Validation failures from the shared validators propagate unchanged, including invalid_usage for an unsupported host and unsafe_path for mode/owner/ancestor violations. Use the Task 2 sbom_failure and checksum_failure categories only for their post-validation operational work. Tests must force raw-input read/parse/build-info/output-write failures and asset read/output-write failures respectively, asserting only the stable category and cleanup behavior.

- [ ] **Step 1: Write pure SPDX RED fixtures**

Create an intentionally incomplete Syft-style SPDX 2.3 skeleton used by rejection tests:

    {
      "spdxVersion": "SPDX-2.3",
      "dataLicense": "CC0-1.0",
      "SPDXID": "SPDXRef-DOCUMENT",
      "creationInfo": {"creators": ["Tool: syft-1.50.0"]},
      "packages": [],
      "relationships": []
    }

Feed finalizeSPDX a synthetic five-binary catalog containing target name, SHA-256, GoVersion, main module, and dependencies. Assert it rejects empty packages/relationships, absence of the exact `Tool: syft-1.50.0` creator entry, duplicate JSON keys, duplicate SPDX IDs, dangling relationships, zero/multiple staging-root packages, a raw extra/missing/duplicate target file, a raw file hash mismatch, one absent per-target dependency or stdlib instance, one package not connected to its evidence file, an unexpected evidence package, a malformed/cross-target stdlib edge, a non-Windows/duplicate/malformed binary-classifier package, a replacement module, wrong dependency or Go version, a purl path-prefix collision, an encoded-purl collision, one omitted target, wrong target hash, and strings containing the repository/staging root or standard Linux, macOS, and Windows user-home prefixes in the finalized projection. Construct those sensitive prefixes from path components in test code so repository hygiene does not contain a literal developer path. Additional well-formed non-tool creator entries may be present in raw Syft output but are never copied.

sbom_test.go and checksum_test.go begin with `//go:build linux || darwin`; unsupported-host behavior lives only in the self-contained host_unsupported_test.go cases and must not depend on helpers from these supported-host files.

Check in a path-redacted fixture preserving the actual Syft 1.50.0 five-binary directory-scan structure: a path-derived document name/namespace, one path-derived root package reached by DOCUMENT DESCRIBES, five root-relative binary file records, repeated per-target module and stdlib package instances, the one Windows binary-classifier package, exact package-to-file OTHER relationships with comment `evident-by: indicates the package's existence is evident by the given file`, and `Tool: syft-1.50.0`. Assert finalization discards rather than copies all raw root/file/relationship nodes, discards the validated Windows classifier, deduplicates final module/stdlib tuples, and emits no fixture root marker. Include equivalent root/file containment fixtures using inverse CONTAINED_BY direction.

The accepted fixture must emit exactly five total file records:

    ./linux_amd64/ai-cli-gateway
    ./linux_arm64/ai-cli-gateway
    ./darwin_amd64/ai-cli-gateway
    ./darwin_arm64/ai-cli-gateway
    ./windows_amd64/ai-cli-gateway.exe

Each record has a fixed SPDXRef-File-ai-cli-gateway-OS-ARCH ID, one SHA256 checksum, NOASSERTION license/copyright fields, and a DOCUMENT DESCRIBES relationship. Each file also has CONTAINS relationships to the exact modules found by buildinfo in that binary; packages use deterministic hash-derived SPDX IDs, and all arrays are sorted by their closed keys.

- [ ] **Step 2: Run SPDX RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack -run 'TestFinalizeSPDX|TestWriteSBOM' -count=1

Expected: compile failure because SBOMOptions, WriteSBOM, and finalizeSPDX are absent.

- [ ] **Step 3: Implement bounded duplicate-safe parsing and build-info union**

Read the raw SPDX through io.LimitReader with a 16 MiB plus one-byte ceiling and parse with safejson.Parse using MaxDepth 64 and MaxNumberBytes 128. Traverse map[string]any values without echoing data in errors.

Call validateRootSet with allowAbsentOutput false, validateRepositoryAndStaging, and the exact tag/source-time validator from Task 2. Separately require OutputRoot to contain exactly the five expected archives and RawPath to be an absolute non-symlink regular file outside RepositoryRoot, StagingRoot, and OutputRoot. This validation must not reuse newArchivePlan because release output is intentionally nonempty.

Add a supported-host regression table that calls WriteSBOM directly with an output root changed in turn to mode 0755, a foreign synthetic owner through the Unix authority seam, and a group/other-writable non-sticky ancestor. Every case must return unsafe_path before reading RawPath or build info, proving WriteSBOM actually invokes the shared validator first.

For each exact staged binary:

- compute SHA-256 from a regular non-symlink file;
- call buildinfo.ReadFile;
- require Main.Path == github.com/krkarma777/ai-cli-gateway;
- require Main.Version == `(devel)` and GoVersion exactly `go1.26.5`, reject any dependency with Replace != nil, and collect Main, stdlib at GoVersion, plus every dependency Path and Version into a deduplicated sorted union while preserving the per-binary membership map.

Identify exactly one raw synthetic root package through DOCUMENT DESCRIBES plus package.name exactly equal to StagingRoot. Require raw files to contain exactly one record for each root-relative target path such as `linux_amd64/ai-cli-gateway` and no extras, require fileTypes to contain BINARY, and require each record's SHA256 to equal the independently reopened staged binary. Never join a raw filename to disk; compare it to the fixed target table and open only the validated staged path.

For every target, follow only incoming package-to-file relationships whose type is OTHER and whose comment is exactly `evident-by: indicates the package's existence is evident by the given file`. Require the related Go package instances to match exactly that binary's buildinfo main/dependency tuples plus stdlib—no missing or unexpected Go instance. Every module instance name must equal the full module path and have exactly one PACKAGE-MANAGER purl external reference that parses to type golang plus that full decoded path. For the main `(devel)` tuple, require versionInfo exactly UNKNOWN and a versionless purl. For each dependency, require versionInfo and the decoded purl version both equal the exact buildinfo version. For stdlib require name `stdlib`, versionInfo equal full GoVersion, and purl `pkg:golang/stdlib@VERSION` where VERSION is GoVersion without the leading `go`.

Permit exactly one additional evidence package globally and only on windows_amd64: name `ai-cli-gateway`, versionInfo UNKNOWN, no PACKAGE-MANAGER purl, an SPDX ID beginning `SPDXRef-Package-binary-ai-cli-gateway-`, one exact evidence edge to the Windows file, one root containment edge, and no other relationship. Validate and discard it. Reject query qualifiers, subpaths, malformed percent encoding, prefix matches, zero per-target candidates, duplicate evidence edges, and an evidence package tied to multiple target files. Identical module/stdlib tuples across targets are expected and are deduplicated only when constructing final packages.

Validate every raw SPDX ID and relationship endpoint for uniqueness/syntax before traversal. Normalize semantic direction for root/file CONTAINS and CONTAINED_BY edges, keep the exact OTHER evidence direction above, and require every target file plus evidence package instance to be reachable from SPDXRef-DOCUMENT through the root/file graph. For each stdlib instance require exactly one DEPENDENCY_OF edge to that target's validated main-module instance and no cross-target reuse. Permit other dependency edges only when both endpoints are validated package instances for the same target. Reject duplicate normalized relationship triples and unrelated endpoints.

Do not mutate or copy the raw document/root package/files/relationships. Construct a closed SPDX 2.3 document with name `ai-cli-gateway-VERSION-five-target-collection`, namespace `https://github.com/krkarma777/ai-cli-gateway/releases/download/TAG/ai-cli-gateway_VERSION_sbom.spdx.json`, creation time SourceTime in RFC3339, and creator `Tool: syft-1.50.0`. Project each deduplicated main/dependency/stdlib component into fixed package fields with SPDX ID `SPDXRef-Package-` plus the full lowercase SHA-256 of identity, one NUL byte, and normalized version, and retain only its canonical validated purl. Do not project the classifier. Then add the five fixed file records and sorted DOCUMENT DESCRIBES plus file CONTAINS relationships.

Recursively scan the completed value and reject any occurrence of the exact repository, staging, or output absolute root strings, any Unix or Windows user-home prefix, control character, or invalid UTF-8 string. Marshal with json.MarshalIndent plus one trailing newline and create the final asset with O_CREATE|O_EXCL and mode 0644.

- [ ] **Step 4: Write checksum RED tests**

After five archives plus ai-cli-gateway_0.1.0_sbom.spdx.json exist, pass the same repository/staging/output roots and assert WriteChecksums writes exactly six lexical records:

    HEX *ai-cli-gateway_0.1.0_darwin_amd64.tar.gz
    HEX *ai-cli-gateway_0.1.0_darwin_arm64.tar.gz
    HEX *ai-cli-gateway_0.1.0_linux_amd64.tar.gz
    HEX *ai-cli-gateway_0.1.0_linux_arm64.tar.gz
    HEX *ai-cli-gateway_0.1.0_sbom.spdx.json
    HEX *ai-cli-gateway_0.1.0_windows_amd64.zip

Reject a relative, symlinked, or overlapping root independently in the checksum path, plus a missing asset, extra file, symlink, malformed asset name, and existing SHA256SUMS. After successful manifest creation, mutate one test-owned asset and require an independent test verifier that reparses SHA256SUMS and reopens every subject to detect the mismatch; WriteChecksums itself intentionally treats the six validated current files as the digest source and has no prior-digest oracle.

- [ ] **Step 5: Implement exact checksum output**

Re-run validateRootSet with allowAbsentOutput false, validateRepositoryAndStaging, and validateTag before enumerating output. Require exactly the six expected regular files. Hash through streaming sha256.New, sort by basename, and write one lowercase 64-hex binary-mode record per line to an O_CREATE|O_EXCL 0644 SHA256SUMS file.

Mirror the supported-host mode-0755, foreign-owner seam, and unsafe-ancestor table through WriteChecksums; each case returns unsafe_path before enumerating or hashing an asset.

- [ ] **Step 6: Write CLI RED tests**

Call run(args, stderr) directly. Cover missing/duplicate flags, relative paths, unknown subcommand, trailing argument, invalid source epoch, and each categorized package error. Assert stderr matches:

    releasepack: invalid_tag

or another closed category and never contains the supplied path, tag text, OS error, raw SPDX, or environment value.

- [ ] **Step 7: Implement the three closed subcommands**

Use flag.NewFlagSet with ContinueOnError and io.Discard output. Required forms are:

    releasepack archives --repository-root ABS --staging-root ABS --output-root ABS --tag v0.1.0 --source-epoch 1785805793
    releasepack sbom --repository-root ABS --staging-root ABS --output-root ABS --raw-sbom ABS --tag v0.1.0 --source-epoch 1785805793
    releasepack checksums --repository-root ABS --staging-root ABS --output-root ABS --tag v0.1.0

Reject repeated flags with a custom flag.Value whose Set method counts every occurrence (FlagSet.Visit is insufficient because it reports each flag only once). Require long-form flags only, no positional arguments, convert epoch with time.Unix(epoch, 0).UTC, and call only the exported functions above. Unknown subcommands, help, missing/repeated/unknown flags, and trailing arguments map to invalid_usage; validated bad tags and package inputs keep their package categories.

main_test.go begins with `//go:build linux || darwin`. main_unsupported_test.go begins with `//go:build !linux && !darwin`; it invokes each of the exact archives, sbom, and checksums command forms with valid-shaped platform-absolute but nonexistent paths and asserts invalid_usage, proving every command reaches the host guard before filesystem inspection. In the releasepack package, Task 4 extends host_unsupported_test.go to call WriteSBOM and WriteChecksums with the same property. Do not silently skip tests; the command and tests must compile and the fail-closed cases must execute on Windows.

- [ ] **Step 8: Run GREEN and a command-level archive dry run**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack/... -count=1
    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/releasepack/... -count=10
    GOOS=windows GOARCH=amd64 GOCACHE=/private/tmp/spawngate-go-build go test -c -o /private/tmp/releasepack-library-task4-windows.test ./internal/releasepack
    GOOS=windows GOARCH=amd64 GOCACHE=/private/tmp/spawngate-go-build go test -c -o /private/tmp/releasepack-cli-task4-windows.test ./internal/releasepack/cmd/releasepack

Then build the CLI outside the repository and invoke archives against a test-owned fixture created by the test binary:

    go build -trimpath -o /private/tmp/releasepack ./internal/releasepack/cmd/releasepack
    /private/tmp/releasepack --help

Expected: Linux/macOS tests PASS, both Windows library/test and command/test compiles PASS, and top-level --help exits nonzero with only the fixed usage category rather than dumping flag internals.

- [ ] **Step 9: Commit**

    git add internal/releasepack/errors.go internal/releasepack/validate.go internal/releasepack/host_unsupported_test.go internal/releasepack/sbom.go internal/releasepack/sbom_test.go internal/releasepack/testdata/syft-1.50.0-five-binaries.spdx.json internal/releasepack/checksum.go internal/releasepack/checksum_test.go internal/releasepack/cmd/releasepack/main.go internal/releasepack/cmd/releasepack/main_test.go internal/releasepack/cmd/releasepack/main_unsupported_test.go
    git commit -m "feat: verify release SBOM and checksums"

### Task 5: Add a Fail-Closed Codex-Ready Fake Executable

**Files:**
- Modify: internal/testcli/fake.go
- Modify: internal/testcli/fake_test.go
- Create: internal/testcli/cmd/fake-codex-cli/main.go

**Interfaces:**
- Consumes: the exact readiness and execution argv in internal/provider/codex/codex.go.
- Produces:

    func CodexReadyMain(
        args []string,
        stdin io.Reader,
        stdout io.Writer,
        stderr io.Writer,
    ) int

    type CodexFinalHandler func(io.Reader, io.Writer, io.Writer) int

    func CodexReadyMainWithFinal(
        args []string,
        stdin io.Reader,
        stdout io.Writer,
        stderr io.Writer,
        final CodexFinalHandler,
    ) int

Existing Main also accepts --mode=codex-ready and delegates to the same matcher after removing only its mode flag.
CodexReadyMain calls CodexReadyMainWithFinal with the fixed immediate SDK_GATEWAY_OK handler. The callback is reached only after the same exact final argv match and exists so Task 8 can build a test-only blocking descendant fixture without duplicating the Codex probe protocol.

- [ ] **Step 1: Write RED tests for the five probe commands**

Call CodexReadyMain directly and require exact behavior:

    --version
        stdout: codex-cli 0.146.0 plus newline
        exit: 0

    --ask-for-approval never exec --help
        stdout contains each independent required help token
        exit: 0

    features list
        one exact “FEATURE_NAME stable false” line for each of the sixteen required hardening feature names, in the listed order
        exit: 0

    login status
        stdout: Logged in plus newline
        exit: 0

    doctor --json
        stdout: {"schemaVersion":1,"overallStatus":"ok","checks":{"auth.credentials":{"id":"auth.credentials","status":"ok"},"config.load":{"id":"config.load","status":"ok"}}} plus newline
        exit: 0

Require empty stderr and reject a reordered, missing, extra, or prefix-matching argument with exit 2 and exactly:

    fake-codex-cli: unsupported command

- [ ] **Step 2: Write RED tests for exact final execution**

Copy the current Codex text argv from fixedBuildArgs and append sdk-contract-model and dash. Feed nonempty private stdin and assert:

    exit == 0
    stdout == "SDK_GATEWAY_OK\n"
    stderr == ""

Assert the prompt is absent from stdout/stderr. Empty stdin, an added --output-schema, another model, missing terminal dash, or any mode flag passed to CodexReadyMain must fail closed without returning output.

Add one test that Main with --mode=codex-ready plus the exact final argv produces the same result and one test that the dedicated command package builds.

- [ ] **Step 3: Run semantic RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/testcli -run 'TestCodexReady|TestMainCodexReady' -count=1

Expected: compile failure because CodexReadyMain does not exist.

- [ ] **Step 4: Implement one exact argv matcher**

Use slices.Equal against fixed slices for the five probes and final execution. Reject a nil final handler; invoke a non-nil handler only after the exact final argv match and bounded prompt validation. Keep the feature list in one fixed array:

    shell_tool
    unified_exec
    code_mode_host
    apps
    plugins
    remote_plugin
    hooks
    multi_agent
    browser_use
    browser_use_external
    computer_use
    in_app_browser
    image_generation
    skill_search
    skill_mcp_dependency_install
    workspace_dependencies

For final execution, read stdin through an 8 MiB plus one-byte limit, require 1..8 MiB valid UTF-8 bytes, discard the bytes, and write only SDK_GATEWAY_OK. Do not inspect environment variables or filesystem state.

The dedicated main is:

    package main

    import (
        "os"

        "github.com/krkarma777/ai-cli-gateway/internal/testcli"
    )

    func main() {
        os.Exit(testcli.CodexReadyMain(os.Args[1:], os.Stdin, os.Stdout, os.Stderr))
    }

- [ ] **Step 5: Verify fake, adapter integration, and trimmed-path behavior**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/testcli ./internal/provider/codex -count=1
    GOCACHE=/private/tmp/spawngate-go-build go test -trimpath ./internal/testcli ./internal/provider/codex -count=1
    go build -trimpath -o /private/tmp/fake-codex-cli ./internal/testcli/cmd/fake-codex-cli

Expected: PASS and the binary is created only under /private/tmp.

- [ ] **Step 6: Commit**

    git add internal/testcli/fake.go internal/testcli/fake_test.go internal/testcli/cmd/fake-codex-cli/main.go
    git commit -m "test: add ready Codex CLI fixture"

### Task 6: Ship and Decode a Minimal Codex Configuration

**Files:**
- Create: examples/config/codex.example.toml
- Modify: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes: config.Decode and its existing defaults.
- Produces: one single-provider, single-model template with four explicit operator substitutions.

- [ ] **Step 1: Write the absent-file RED contract**

Add TestCodexConfigExampleContract to internal/securitytest. Parse TOML and require exact keys:

    root: server, runtime, providers, models
    server: listen, api_key_env
    runtime: root
    providers: codex
    providers.codex: executable, config_home
    models[0]: id, provider, provider_model, created

Require each marker exactly once:

    /opt/ai-cli-gateway/bin/codex
    /var/lib/ai-cli-gateway/codex-home
    /var/lib/ai-cli-gateway/runtime
    configured-provider-model

Require no prefix_args, credential_env, provider limits, real home path, account identity, token, auth filename, or second provider/model.

- [ ] **Step 2: Write native decode RED assertions**

On Unix decode the file unchanged and assert:

    Server.Listen == "127.0.0.1:8080"
    Server.APIKeyEnv == "AI_CLI_GATEWAY_API_KEY"
    Runtime.Root == "/var/lib/ai-cli-gateway/runtime"
    Providers["codex"].Executable == "/opt/ai-cli-gateway/bin/codex"
    Providers["codex"].ConfigHome == "/var/lib/ai-cli-gateway/codex-home"
    Providers["codex"].PrefixArgs is empty
    Providers["codex"].CredentialEnv is empty
    provider concurrency/queue/timeouts equal decoder defaults
    Models equals codex-local -> codex/configured-provider-model

On Windows replace the four markers exactly once with:

    C:/ai-cli-gateway/bin/codex.exe
    C:/ProgramData/ai-cli-gateway/codex-home
    C:/ProgramData/ai-cli-gateway/runtime
    sdk-contract-model

Decode the test-local string and assert native absolute paths and identical defaults.

- [ ] **Step 3: Run RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run 'TestCodexConfigExample' -count=1

Expected: FAIL because examples/config/codex.example.toml is absent.

- [ ] **Step 4: Add the minimal template**

Use this complete TOML shape:

    # Replace all four generic values before serving.
    [server]
    listen = "127.0.0.1:8080"
    api_key_env = "AI_CLI_GATEWAY_API_KEY"

    [runtime]
    root = "/var/lib/ai-cli-gateway/runtime"

    [providers.codex]
    executable = "/opt/ai-cli-gateway/bin/codex"
    config_home = "/var/lib/ai-cli-gateway/codex-home"

    [[models]]
    id = "codex-local"
    provider = "codex"
    provider_model = "configured-provider-model"
    created = 0

The comments must state that the model marker is not a provider default or entitlement claim and must be replaced with a model accessible to the already authenticated CLI.

- [ ] **Step 5: Verify Unix, repository hygiene, and Windows compilation**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run 'TestCodexConfigExample|TestRepositoryHygiene' -count=1
    GOOS=windows GOARCH=amd64 go test -c -o /private/tmp/securitytest-windows.test ./internal/securitytest

Expected: PASS and Windows test compilation succeeds.

- [ ] **Step 6: Commit**

    git add examples/config/codex.example.toml internal/securitytest/repository_test.go
    git commit -m "docs: add minimal Codex configuration"

### Task 7: Add Official OpenAI Python and JavaScript Contract Clients

**Files:**
- Create: examples/openai-sdk/python/main.py
- Create: examples/openai-sdk/python/requirements.txt
- Create: examples/openai-sdk/python/requirements.lock
- Create: examples/openai-sdk/javascript/main.mjs
- Create: examples/openai-sdk/javascript/package.json
- Create: examples/openai-sdk/javascript/package-lock.json
- Modify: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes required environment variables AI_CLI_GATEWAY_BASE_URL, AI_CLI_GATEWAY_API_KEY, and AI_CLI_GATEWAY_MODEL plus optional AI_CLI_GATEWAY_TIMEOUT_SECONDS.
- Produces: two executable examples that make models.list and one non-streaming responses.create request, assert the complete supported shape, and emit only fixed success/error categories.

- [ ] **Step 1: Write the SDK file-contract RED**

Add TestOfficialSDKExamplesContract. It must:

- parse requirements.txt and require exactly openai==2.53.0;
- require every noncomment requirements.lock line to be an exact name==version pin, include openai==2.53.0, and contain no URL, editable install, local path, token, or provider credential;
- parse package.json and require type module, engines.node >=24, and dependencies.openai exactly 6.48.0;
- parse package-lock.json, require lockfileVersion 3, root openai 6.48.0, integrity fields for packages, and no lifecycle script in the root package;
- require both source files to disable retries, parse only ASCII decimal AI_CLI_GATEWAY_TIMEOUT_SECONDS in the closed range 1..300 with default 300, call models and responses, and name only those four gateway variables;
- reject SDK debug logging, streaming true, a nonempty tools array, and tool execution code.

- [ ] **Step 2: Run the absent-file RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run TestOfficialSDKExamplesContract -count=1

Expected: FAIL on the first absent SDK file.

- [ ] **Step 3: Add exact dependency manifests and generate locks outside the repository cache**

requirements.txt:

    openai==2.53.0

package.json:

    {
      "name": "ai-cli-gateway-openai-sdk-example",
      "private": true,
      "type": "module",
      "engines": {"node": ">=24"},
      "dependencies": {"openai": "6.48.0"}
    }

Create a Python 3.12 virtual environment below /private/tmp, install requirements.txt with --disable-pip-version-check and --no-input, then generate a sorted pip freeze --local result as requirements.lock. Generate package-lock.json with:

    NPM_CONFIG_CACHE=/private/tmp/spawngate-npm-cache npm install --package-lock-only --ignore-scripts --prefix examples/openai-sdk/javascript

Inspect both generated locks, then run repository hygiene before staging them.

- [ ] **Step 4: Implement the Python example**

Validate environment before constructing OpenAI. Instantiate:

    client = OpenAI(**{
        "api_key": require_env("AI_CLI_GATEWAY_API_KEY"),
        "base_url": require_env("AI_CLI_GATEWAY_BASE_URL"),
        "timeout": timeout_seconds(),
        "max_retries": 0,
    })

The dictionary form is deliberate: it remains the official constructor contract
while avoiding a source line that resembles a literal secret assignment to the
repository scanner.

Call client.models.list and require one exact codex-sdk-test model. Then call:

    client.responses.create(
        model=require_env("AI_CLI_GATEWAY_MODEL"),
        instructions="SDK contract instruction.",
        input="SDK contract input.",
        text={"format": {"type": "text"}},
        stream=False,
        store=False,
        tools=[],
        tool_choice="none",
    )

Assert _request_id starts req_, response id starts resp_, one message id starts msg_, all fixed/null request echoes, one completed assistant message, one output_text with empty annotations, exact SDK_GATEWAY_OK newline, and completed_at >= created_at.

Implement timeout_seconds so absent means 300.0 and only an ASCII decimal integer from 1 through 300 is accepted. Wrap main so success prints python_sdk_contract_ok. Missing environment prints sdk_contract_error: missing NAME; an invalid optional timeout prints sdk_contract_error: invalid AI_CLI_GATEWAY_TIMEOUT_SECONDS. API failures print only sdk_contract_error: python_api STATUS. Assertion or other failures print only sdk_contract_error: python_assertion. Never print exception text, headers, request bodies, IDs, or values.

- [ ] **Step 5: Implement the JavaScript example**

Instantiate:

    const client = new OpenAI({
      apiKey: requireEnv("AI_CLI_GATEWAY_API_KEY"),
      baseURL: requireEnv("AI_CLI_GATEWAY_BASE_URL"),
      timeout: timeoutMilliseconds(),
      maxRetries: 0,
    });

Implement timeoutMilliseconds with the same absent/default and ASCII-decimal 1..300 contract, multiplied by 1,000 only after validation. Make the same two calls and exact assertions with model set from AI_CLI_GATEWAY_MODEL. Require response._request_id plus the same response/message/content shape. Success prints javascript_sdk_contract_ok; fixed error output is sdk_contract_error: javascript_api STATUS, sdk_contract_error: javascript_assertion, sdk_contract_error: invalid AI_CLI_GATEWAY_TIMEOUT_SECONDS, or sdk_contract_error: missing NAME. Do not serialize an SDK exception.

- [ ] **Step 6: Verify syntax, manifests, and hygiene**

Run:

    PYTHONPYCACHEPREFIX=/private/tmp/spawngate-pycache python3 -m py_compile examples/openai-sdk/python/main.py
    node --check examples/openai-sdk/javascript/main.mjs
    mkdir -p /private/tmp/spawngate-sdk-js-check
    cp examples/openai-sdk/javascript/main.mjs examples/openai-sdk/javascript/package.json examples/openai-sdk/javascript/package-lock.json /private/tmp/spawngate-sdk-js-check/
    NPM_CONFIG_CACHE=/private/tmp/spawngate-npm-cache npm ci --ignore-scripts --prefix /private/tmp/spawngate-sdk-js-check
    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run 'TestOfficialSDKExamplesContract|TestRepositoryHygiene' -count=1

Remove only the validated /private/tmp/spawngate-sdk-js-check directory, then rerun TestRepositoryHygiene to prove no generated dependency was written into the repository.

- [ ] **Step 7: Commit**

    git add examples/openai-sdk internal/securitytest/repository_test.go
    git commit -m "docs: add official OpenAI SDK examples"

### Task 8: Run Both SDKs Through the Real Gateway and Fake Codex

**Files:**
- Create: internal/sdkcontract/runner.go
- Create: internal/sdkcontract/runner_test.go
- Create: internal/sdkcontract/system.go
- Create: internal/sdkcontract/system_test.go
- Create: internal/sdkcontract/child_unix.go
- Create: internal/sdkcontract/child_windows.go
- Create: internal/sdkcontract/registry_unix.go
- Create: internal/sdkcontract/registry_windows.go
- Create: internal/sdkcontract/cmd/sdk-contract/main.go
- Create: internal/sdkcontract/cmd/sdk-contract/main_test.go
- Create: internal/sdkcontract/cmd/sdk-contract/signals_unix.go
- Create: internal/sdkcontract/cmd/sdk-contract/signals_windows.go
- Create: internal/sdkcontract/testdata/slow-codex/main.go
- Create: scripts/sdk-contract.sh
- Modify: README.md
- Modify: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes: the checked-in SDK examples, the dedicated fake Codex command, the real gateway command, and the minimal config contract.
- Produces:

    type Options struct {
        RepositoryRoot       string
        PythonExecutable     string
        NodeExecutable       string
        JavaScriptEntrypoint string
    }

    func Run(context.Context, Options, io.Writer) error
    func ErrorCategory(error) string

Internal runWithSystem(context.Context, Options, io.Writer, system, lifecyclePolicy) drives injectable system and time/grace boundaries. Production policy fixes poll interval 100 ms, readiness five seconds, probe one second, gateway grace ten seconds, and bounded helper, registry-protocol, registry-cleanup, and descendant-readiness grace values. Unit tests inject fake timers and zero/short grace; the real forced-kill integration uses production values exactly once. Max startup attempts is exactly eight.

Use these exact orchestration boundaries:

    type child interface {
        PID() int
        Exited() <-chan struct{}
        StopAndWait(time.Duration) cleanupResult
    }

    type ownedRoot interface {
        Path() string
        RemoveExact() error
        Close() error
    }

    type cleanupResult struct {
        SafeToRemove bool
        Err          error
    }

    type cleanupSafetyError interface {
        error
        SafeToRemove() bool
    }

    type timer interface {
        C() <-chan time.Time
        Stop() bool
    }

    type lifecyclePolicy struct {
        PollInterval        time.Duration
        ReadinessDeadline   time.Duration
        ProbeTimeout        time.Duration
        GatewayGrace        time.Duration
        HelperGrace         time.Duration
        RegistryProtocol    time.Duration
        RegistryCleanup     time.Duration
        DescendantReadiness time.Duration
    }

    type fixtureRegistry interface {
        Ready() <-chan struct{}
        StopAndVerify(time.Duration) cleanupResult
    }

    type system interface {
        Supported() bool
        MkdirTemp(parent, pattern string) (ownedRoot, error)
        MkdirAll(path string, mode fs.FileMode) error
        WriteFile(path string, data []byte, mode fs.FileMode) error
        ReadRandom(destination []byte) (int, error)
        NewTimer(time.Duration) timer
        Build(context.Context, string, string, string) error
        AllocatePort(context.Context, string) (uint16, error)
        StartFixtureRegistry(string) (fixtureRegistry, error)
        StartGateway(string, string, []string, []string, io.Writer) (child, error)
        ProbeModels(context.Context, string, string) (int, error)
        RunClient(context.Context, string, []string, []string) ([]byte, error)
    }

Build arguments are repository root, output path, and fixed package path. StartGateway arguments are executable, working directory, argv, environment, and combined bounded output. PID is diagnostic state for test-owned liveness checks and is never logged or returned in an error. Successful Build, AllocatePort, and RunClient returns certify their owned Wait was joined and inherited process group is absent. A nonnil value returned by MkdirTemp, StartFixtureRegistry, or StartGateway transfers ownership even when accompanied by an error and must be cleaned; a nil value plus a plain error certifies that the constructor rolled back every artifact, goroutine, process, descriptor, and timer. Rollback uncertainty or a helper error after process start must return a cleanupSafetyError, not a plain error, and its SafeToRemove value records whether absence was proven. The orchestration layer owns a retained-identity temp root immediately after MkdirTemp returns one, one root-scoped registry immediately after StartFixtureRegistry returns one, and a child immediately after StartGateway returns one. It calls StopAndWait exactly once for every owned child, including early-exit and readiness-timeout attempts. Exited is observation only; the real child implementation owns the single Cmd.Wait call and StopAndWait joins it idempotently. Failed attempts remain inside the private root rather than using pathname-only recursive removal.

The registry is root-scoped: start it once before the first gateway attempt, keep it active through at most eight readiness attempts, and call StopAndVerify exactly once only during terminal cleanup. Retiring a retryable attempt stops only that attempt's gateway. An untouched zero-byte/zero-record registry is valid for the ordinary fake, any pre-protocol terminal failure, and exhausted readiness. Normal orchestration never calls Ready. Calling Ready is integration-only and atomically arms the requirement for a complete protocol; observing the first protocol byte also arms it. The first arming event starts the protocol deadline. Once armed, absent, partial, missing-continuation, malformed, duplicate, oversized, or timed-out input fails closed, including an integration Ready wait that times out before any byte arrives.

Terminal cleanup stops the final owned gateway, then finalizes the registry. Track cleanupResult.SafeToRemove for gateway/registry and cleanupSafetyError.SafeToRemove for Build, AllocatePort, and RunClient. If every started helper, gateway, and registered group is proven absent, call RemoveExact even when a cleanup error occurred; RemoveExact closes the retained root handle on every success or failure return. If any group is live or unverified, never call RemoveExact: call the root's idempotent Close instead, retain the private root for manual recovery, and return only sdk_contract_cleanup_failed without a path. Every remaining cleanup step that is safe is attempted after a failure. Primary validation/build/readiness/SDK/cancellation errors retain their category only when all cleanup and removal succeed; cleanup failure takes precedence and stops retries.

- [ ] **Step 1: Write state-machine RED tests with a fake system**

The fake system records allocations, starts, authenticated probes, client invocations, TERM/KILL, waits, and removals. Cover:

- success on the first port;
- first port occupied/early gateway exit, then success on the second fresh attempt;
- a readiness deadline with a still-running child calls StopAndWait before retry;
- all eight attempts fail and no ninth allocation occurs;
- one registry is created before attempt one, remains active across all eight failures, accepts zero bytes/records, and is finalized once only in terminal cleanup; retry cleanup stops only its gateway;
- normal Run never calls Ready; the slow integration's Ready call arms mandatory completion, and a timeout before any byte fails closed;
- after a Ready call or the first protocol byte, absent, partial, missing-continuation, malformed, duplicate, oversized, or protocol-timeout input fails closed;
- cancellation during readiness stops and reaps once and never retries;
- Python failure prevents JavaScript execution;
- JavaScript failure after Python success;
- TERM exits within grace;
- TERM timeout triggers KILL and wait;
- Build, AllocatePort, Python, and Node cancellation each TERM/KILL their own process group, join Wait exactly once, and leave no inherited-group descendant;
- cleanup error with every helper/gateway/fixture group proven absent still calls RemoveExact, closes the retained handle, and wins over an otherwise successful run;
- a live or unverified helper, gateway, or registered fixture group skips RemoveExact, calls root Close, retains the private recovery root, and returns cleanup_failed without a path;
- cleanup ownership transitions before/after root, registry, gateway, readiness, Python, JavaScript, and final removal preserve the precedence and exactly-once rules;
- constructor faults after each allocation either roll back fully before a nil/plain-error return or return the nonnil recovery owner plus cleanup-safety failure; no unowned artifact, goroutine, process, descriptor, or timer remains;
- a short entropy read and entropy error fail before child startup;
- Python/Node leaf aliases are validated through their resolved targets and the original aliases are passed to AllocatePort and RunClient; replacing an alias, resolved target, or JavaScript leaf after validation prevents the replacement from executing;
- exact-root replacement before cleanup preserves the replacement and returns cleanup_failed;
- both client environments contain AI_CLI_GATEWAY_TIMEOUT_SECONDS=5 and no inherited timeout/debug variable;
- every generated config decodes with config.Decode and contains its selected loopback port, fake executable, unique config/runtime roots, server shutdown_timeout 8s, runtime term_grace 1s and cleanup_timeout 2s, model id codex-sdk-test, and provider_model sdk-contract-model;
- restrictive-umask creation is followed by chmod and verification of exact 0700 directories and 0600 config/registry files;
- injected timers and lifecycle grace reach readiness, helper TERM/KILL, registry protocol, registry cleanup, and descendant-readiness boundaries without production wall-clock delay and are stopped on every path;
- log longer than 64 KiB remains memory-bounded while the entire stream is drained;
- forbidden values split across writes or appearing only after 64 KiB are detected and suppressed, including the generated key, instruction, input, SDK_GATEWAY_OK, executable path, config path, and runtime root; and
- unsupported_platform occurs before temporary-root creation or other side effects.

Each attempt must use a distinct config home, runtime root, config file, and in-memory deferred log capture. No raw gateway log file is written.

- [ ] **Step 2: Run state-machine RED**

For Task 8 local macOS/Linux commands, create one private host temporary root in
the current shell rather than assuming `/private/tmp`:

    SDK_CONTRACT_TMP_PARENT="${TMPDIR:-/tmp}"
    SDK_CONTRACT_HOST_TMP="$(mktemp -d "${SDK_CONTRACT_TMP_PARENT%/}/spawngate-sdk-contract.XXXXXX")"
    chmod 0700 "${SDK_CONTRACT_HOST_TMP}"

Run:

    GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test ./internal/sdkcontract -run TestRun -count=1

Expected: compile failure because the package and Run do not exist.

- [ ] **Step 3: Implement closed options and retry orchestration**

On Windows return unsupported_platform before entropy, temporary-root creation, or process startup; retain build-tagged stubs so the package and command cross-compile. On macOS/Linux validate all four paths as absolute and clean. Record Python/Node alias-leaf and resolved-target paths and identities. For the alias path, resolved path, and JavaScript path, require every ancestor to be a non-symlink directory owned by root or the effective UID; reject group/other-writable ancestors unless sticky, exactly matching the release/private-root authority rule. Require non-group/other-writable regular executable targets, and re-resolve/revalidate immediately before AllocatePort or RunClient. This resolution is validation only: retain and execute the original aliases so virtual-environment and launcher behavior is preserved. Record and revalidate the same non-group/other-writable, non-symlink JavaScript regular-file identity immediately before Node. Require repository go.mod to contain the exact module. Same-credential mutation between final validation and exec is outside the v0.1 boundary; detected replacement always fails closed.

The runner must:

1. create a temporary root with os.MkdirTemp(filepath.Dir(repositoryRoot), ".sdk-contract-"), descriptor-chmod and verify exact 0700, retain its identity/open handle, and validate its parent/ancestors as root-or-effective-UID-owned non-symlink directories with group/other-writable ancestors allowed only when sticky;
2. fill exactly 32 bytes with crypto/rand through ReadRandom, reject a zero/short fill or error, lowercase-hex encode them to exactly 64 ASCII characters, and never print the key;
3. build ai-cli-gateway and fake-codex-cli into bin under that root with go build -trimpath, using group-aware cancellation for the build process tree;
4. create and own one verified mode-0600, root-scoped fixture registry FIFO adjacent to the fake executable before any gateway attempt; normal Run never calls Ready, while an integration Ready call arms mandatory bounded protocol completion;
5. ask the supplied original Python alias to allocate 127.0.0.1:0 through AllocatePort, record the concrete port, and close the probe socket;
6. create attempt-N/config-home and runtime with chmod-and-verified exact 0700 and an exclusive config.toml with exact 0600; the generated TOML has the selected listen address and fake executable, shutdown_timeout 8s, term_grace 1s, cleanup_timeout 2s, model id codex-sdk-test, provider_model sdk-contract-model, and no prefix_args or credentials;
7. start the gateway with an empty inherited environment containing only PATH, HOME, and AI_CLI_GATEWAY_API_KEY;
8. poll authenticated /v1/models with injected timers and lifecyclePolicy's 100 ms production interval and hard five-second production deadline;
9. on early exit, readiness deadline, address collision, or other retryable readiness failure, StopAndWait that attempt's gateway and retry with a fresh retained attempt through attempt eight; never finalize the root-scoped registry during retry, cancellation stops and reaps but never retries, and cleanup failure never retries;
10. send missing-key and wrong-key model requests and require 401 without reading their body into diagnostics;
11. invoke the original Python and Node aliases sequentially with only HOME, PATH, base URL, key, AI_CLI_GATEWAY_MODEL=codex-sdk-test, and AI_CLI_GATEWAY_TIMEOUT_SECONDS=5;
12. during terminal cleanup only, terminate/wait the final gateway and finalize the registry exactly once; call RemoveExact only when all helper/gateway/registry cleanup results prove process absence and the root pathname still identifies the retained directory, otherwise Close the lease without removal and retain the private recovery root; report only cleanup_failed and no path.

Use sdk_contract_failed, sdk_contract_canceled, sdk_contract_cleanup_failed, invalid_input, and unsupported_platform as the only external categories.

- [ ] **Step 4: Implement real child and HTTP boundaries**

On Unix every external command uses Setpgid true and a shared group-aware wait owner. StopAndWait sends SIGTERM to the negative process group, waits up to the supplied grace, then sends SIGKILL to the same group and always joins the one Wait result and verifies the non-session-escaping group is absent. The gateway grace is ten seconds, exceeding the generated gateway shutdown_timeout. Build, AllocatePort, and RunClient use shorter bounded TERM/KILL cleanup through the same primitive. A deliberate setsid escape is outside this process-group guarantee. child_windows.go and registry_windows.go are compileable stubs; Windows returns unsupported_platform before a real start and the SDK contract CI remains Linux-only, while local supported execution includes macOS.

AllocatePort executes the original Python alias with the exact fixed argv `-I`, `-c`, and repository-owned code that creates an AF_INET/SOCK_STREAM socket, binds 127.0.0.1:0, prints the port, and closes the socket. Give the helper an empty environment, bound stdout to at most six decimal/newline bytes plus one overflow byte, suppress bounded stderr, require exit zero and exact `[1-9][0-9]{0,4}\n`, parse 1..65535, and never interpolate caller data into code or a shell string. Unit tests record the exact argv/environment and reject overflow, junk, zero, and out-of-range output.

The testdata slow-codex command delegates all matching to testcli.CodexReadyMainWithFinal. Before attempt one the real system creates one mode-0600 root-scoped named-pipe registry adjacent to the test-owned fixture and drains it with bounded exact parsing through every readiness retry. Zero bytes/records is valid for ordinary fake execution, a pre-protocol terminal failure, or readiness exhaustion; normal Run never calls Ready. Calling Ready is integration-only and atomically arms mandatory completion, as does the first protocol byte; the first arming event starts lifecyclePolicy.RegistryProtocol. On entering its final handler the fixture verifies and registers its provider PID/PGID before spawning anything. It then starts one direct-argv ignore-TERM descendant in that inherited provider group, waits through lifecyclePolicy.DescendantReadiness until the descendant has installed its TERM behavior, registers the descendant, closes Ready, ignores TERM, and blocks. Once armed, absent continuation, partial, malformed, duplicate, oversized, or timed-out FIFO records fail closed. If descendant readiness or registration fails, the provider kills and waits that descendant before returning nonzero. The slow integration calls Ready and waits with a separate bounded context; timing out before any byte still taints terminal registry cleanup, and the retry state machine never calls Ready.

The provider process group differs from the gateway group. During terminal cleanup—and never while retiring a retry—the final gateway StopAndWait is followed exactly once by registry StopAndVerify. If a registered provider group remains, send TERM, then KILL, and require the registered group and PIDs to return ESRCH. StopAndVerify returns cleanupResult with explicit SafeToRemove. A nonnil cleanup error with SafeToRemove true still permits RemoveExact; SafeToRemove false skips removal, closes the root lease, and retains the private root for operator recovery without exposing its path. On the graceful path the gateway reaps its provider. After forced gateway death the harness terminates registered non-child fixture processes and observes absence; it must not claim to have waited non-children. The Unix integration test cancels only after registry Ready, exercises forced gateway termination once with production lifecyclePolicy, and proves the gateway Wait was joined and the registered provider/descendant are absent before RemoveExact. This registry is fixture-specific and does not claim containment of arbitrary setsid escape.

Capture gateway stdout/stderr together through a mutex-protected deferred scanner that retains at most 64 KiB, keeps draining and scanning the full stream after overflow, and matches forbidden values across Write boundaries. Raw gateway output and raw log files are never emitted; overflow, scanner failure, or a forbidden match returns only a fixed category. SDK stdout/stderr is bounded to 8 KiB and accepted only when it is exactly the fixed success marker, after which the runner writes that fixed marker itself.

Use http.Client with a one-second per-probe timeout and no proxy:

    Transport: &http.Transport{Proxy: nil}

Never enable SDK or HTTP debug logging.

- [ ] **Step 5: Write command and shell RED contracts**

The Go command accepts exactly:

    --repository-root ABS
    --python ABS
    --node ABS
    --javascript ABS

Unknown, duplicate, or missing flags print sdk-contract: invalid_input only.

The command creates the Run context with signal.NotifyContext. Build-tagged Unix code subscribes to os.Interrupt and SIGTERM; Windows subscribes to os.Interrupt only. Always call the returned stop function. Unit tests inject the runner/context factory, and a Unix signal test delivers SIGTERM and observes context cancellation.

Add a short README recovery note: `sdk_contract_cleanup_failed` can retain an owner-only `.sdk-contract-*` sibling when a process/group or root identity cannot be proven safe. The operator must first ensure no recorded contract process remains, then inspect and remove only the exact retained directory. The command/error output never prints that path or an underlying error. Security tests require this guidance without any developer-specific absolute path.

The shell contract test requires mode 0755, set -eu, exactly three positional arguments, physical repository-root resolution, and one exec of:

    go run -trimpath ./internal/sdkcontract/cmd/sdk-contract

It must pass the four named flags as separate quoted argv elements and contain no eval, sh -c, bash -c, provider credential name, literal gateway key, or request text.

- [ ] **Step 6: Implement the thin shell entrypoint**

Use this control shape:

    #!/bin/sh
    set -eu

    if [ "$#" -ne 3 ]; then
        printf '%s\n' 'usage: scripts/sdk-contract.sh PYTHON NODE JAVASCRIPT' >&2
        exit 2
    fi

    script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
    repository_root=$(CDPATH= cd -- "${script_dir}/.." && pwd -P)
    cd -- "${repository_root}"
    exec go run -trimpath ./internal/sdkcontract/cmd/sdk-contract \
        --repository-root "${repository_root}" \
        --python "$1" \
        --node "$2" \
        --javascript "$3"

Set the committed script mode explicitly:

    chmod 0755 scripts/sdk-contract.sh

- [ ] **Step 7: Run unit GREEN and repository security**

Run:

    GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test ./internal/sdkcontract/... -count=1
    GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test ./internal/sdkcontract/... -count=10
    GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test ./internal/securitytest -run 'TestSDKContractScript|TestRepositoryHygiene' -count=1
    GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test -tags=integration ./internal/sdkcontract -run TestRealForcedGatewayKillRegistryCleanup -count=1
    GOOS=windows GOARCH=amd64 GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test -c -o "${SDK_CONTRACT_HOST_TMP}/sdkcontract-windows.test" ./internal/sdkcontract
    GOOS=windows GOARCH=amd64 GOCACHE="${SDK_CONTRACT_HOST_TMP}/go-build" go test -c -o "${SDK_CONTRACT_HOST_TMP}/sdkcontract-cmd-windows.test" ./internal/sdkcontract/cmd/sdk-contract

Expected: PASS with no sibling temp root remaining. The `-count=10` suite uses fake timers and injected short lifecycle grace; the real forced-kill integration runs once separately with production grace. Unit coverage must include the full ownership/cancellation matrix, root and executable/entrypoint replacement, restrictive umask, helper process trees, split/after-limit secret scanning, FIFO lifecycle/failure modes, safe-remove true/false outcomes, and unsupported-before-temp behavior described above.

- [ ] **Step 8: Run the real black-box contract locally**

Create the Python 3.12 venv and JavaScript workspace below the already created private host temporary root, install requirements.lock, copy main.mjs/package manifests, and run npm ci --ignore-scripts there. Invoke:

    scripts/sdk-contract.sh "${SDK_CONTRACT_HOST_TMP}/python/bin/python" "$(command -v node)" "${SDK_CONTRACT_HOST_TMP}/javascript/main.mjs"

Expected fixed stdout:

    python_sdk_contract_ok
    javascript_sdk_contract_ok

Verify no .sdk-contract-* sibling directory, gateway process, fake CLI process, config.toml, node_modules, or Python cache remains in the repository.

After all Task 8 checks, remove only the unchanged exact root returned by
`mktemp`; never remove its parent or a glob:

    case "${SDK_CONTRACT_HOST_TMP}" in
        "${SDK_CONTRACT_TMP_PARENT%/}"/spawngate-sdk-contract.*) ;;
        *) exit 1 ;;
    esac
    rm -rf -- "${SDK_CONTRACT_HOST_TMP}"

- [ ] **Step 9: Commit**

    git add internal/sdkcontract scripts/sdk-contract.sh internal/securitytest/repository_test.go README.md
    git commit -m "test: exercise official SDK gateway contract"

### Task 9: Make CI Reusable and Enforce the SDK Contract

**Files:**
- Modify: .github/workflows/ci.yml
- Modify: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes: scripts/sdk-contract.sh and both dependency locks from Tasks 7–8.
- Produces: CI supports push, pull_request, and workflow_call; exact job set is lint, linux, macos, windows, cross-build, sdk-contract.

- [ ] **Step 1: Update the exact CI security test first**

Change TestWorkflowMultiPlatformReleaseContract to require:

    on:
      push:
      pull_request:
      workflow_call:

Expected jobs:

    lint
    linux
    macos
    windows
    cross-build
    sdk-contract

Expected action counts:

    actions/checkout@v7: 6
    actions/setup-go@v7: 6
    golangci/golangci-lint-action@v9: 1
    actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97: 1
    actions/setup-node@820762786026740c76f36085b0efc47a31fe5020: 1

Require sdk-contract to use ubuntu-latest, timeout-minutes 12, Python 3.12, Node 24, runner-temporary venv and JavaScript workspace, pip requirements.lock, npm ci --ignore-scripts, and the three-argument script invocation. Keep the existing top-level contents read permission and every existing forbidden token.

- [ ] **Step 2: Run RED against unchanged CI**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run TestWorkflowMultiPlatformReleaseContract -count=1

Expected: FAIL because workflow_call and sdk-contract are absent and action counts are five.

- [ ] **Step 3: Add workflow_call without changing existing jobs**

The trigger block becomes:

    on:
      push:
      pull_request:
      workflow_call:

Keep top-level permissions exactly contents read. Do not add workflow inputs, secrets, concurrency, pull_request_target, artifact upload, or failure bypass.

- [ ] **Step 4: Add the Linux-only SDK job**

Use the existing checkout/setup-go major pins and these full pins for new runtime actions:

    actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
    actions/setup-node@820762786026740c76f36085b0efc47a31fe5020

The preparation step must:

1. create ${RUNNER_TEMP}/sdk-python and install requirements.lock with --disable-pip-version-check and --no-input;
2. create ${RUNNER_TEMP}/sdk-javascript with mode 0700;
3. copy only main.mjs, package.json, and package-lock.json there;
4. run npm ci --ignore-scripts --prefix ${RUNNER_TEMP}/sdk-javascript;
5. invoke scripts/sdk-contract.sh with the venv Python, command -v node, and copied main.mjs.

No generated dependency or binary path may point below GITHUB_WORKSPACE.

- [ ] **Step 5: Run CI contract and complete security suite**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run TestWorkflowMultiPlatformReleaseContract -count=1
    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -count=1

Expected: PASS.

- [ ] **Step 6: Commit**

    git add .github/workflows/ci.yml internal/securitytest/repository_test.go
    git commit -m "ci: verify official SDK compatibility"

### Task 10: Add a Split-Authority, Tag-Only Release Workflow

**Files:**
- Create: .github/workflows/release.yml
- Modify: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes: reusable ci.yml, releasepack command, five cross-build targets, Syft, and GitHub artifact attestations.
- Produces: exact jobs verify, package, asset-verification, and publish with immutable artifact ID handoff and one public draft-first release.

- [ ] **Step 1: Write the absent-workflow RED contract**

Add TestReleaseWorkflowContract and parse release.yml independently. Require:

- trigger only push.tags containing v*.*.*;
- concurrency group release-${{ github.repository }}-${{ github.ref_name }} and cancel-in-progress false;
- exact four jobs and needs chain verify -> package -> asset-verification -> publish;
- verify is a reusable call to ./.github/workflows/ci.yml with contents read; because GitHub forbids timeout-minutes on a reusable-call job, require bounded timeouts on every called CI job instead;
- package permissions exactly contents read, id-token write, attestations write;
- asset-verification permissions exactly contents read, attestations read;
- publish permission exactly contents write;
- package, asset-verification, and publish run on ubuntu-24.04 with timeouts 25, 10, and 10 minutes;
- no checkout or repository command in the last two jobs;
- every external action in release.yml uses the exact full SHA listed below;
- no secrets context, pull_request_target, continue-on-error, clobber, overwrite true, wildcard subject/upload, provider CLI, curl installer, or unpinned tool version.

- [ ] **Step 2: Require exact release operations in the RED test**

The test must find:

- exact SemVer regex, refs/tags validation, tag commit resolution, fetch-depth 0, an explicit +refs/heads/main:refs/remotes/origin/main fetch, origin/main ancestry check, and persist-credentials false;
- five explicit CGO-disabled builds with -trimpath, -buildvcs=false, -mod=readonly, -s -w, and three buildinfo -X assignments;
- go version -m inspection and exact version smoke test;
- releasepack archives, sbom, and checksums invocations;
- Syft v1.50.0 with upload-artifact false, upload-release-assets false, no config input, and step-local SYFT_COMPLIANCE_MISSING_NAME=drop plus SYFT_COMPLIANCE_MISSING_VERSION=stub;
- sha256sum --check --strict in package, asset-verification, and publish;
- actions/attest subject-path with seven exact filenames;
- upload-artifact overwrite false, one-day retention, hidden files false, and seven exact paths;
- download-artifact by artifact-ids with digest-mismatch error in both downstream jobs;
- one explicit seven-subject loop whose gh attestation verify call is constrained by repo, predicate type, signer workflow, source digest, and source ref;
- a successful-null-only GraphQL release(tagName:) preflight with explicit malformed/missing/error fixture rejection;
- live REST resolution of lightweight or exactly one-layer annotated tags to the package commit immediately before draft creation and again immediately before publication, with moved/malformed/nested tag fixture rejection;
- gh release create with --verify-tag, --draft, --title, and --generate-notes;
- gh release edit with --draft=false.

- [ ] **Step 3: Run the absent-workflow RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run TestReleaseWorkflowContract -count=1

Expected: FAIL because .github/workflows/release.yml is absent.

- [ ] **Step 4: Create the verify and package jobs**

Pin actions exactly:

    actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
    actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e
    anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610
    actions/attest@508db95dd578ae2727ebd6217d5ba78e4fbda05d
    actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
    actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c

The package job exports artifact_id, artifact_digest, tag_commit, version, and source_epoch from fixed steps. Checkout uses persist-credentials false and fetch-depth 0. Its shell uses set -euo pipefail. It validates GITHUB_REF_NAME before interpolating it, resolves TAG^{commit}, fetches +refs/heads/main:refs/remotes/origin/main explicitly, and requires git merge-base --is-ancestor TAG_COMMIT refs/remotes/origin/main.

Build the releasepack helper into RUNNER_TEMP, stage five binaries in exact target directories, and run releasepack archives into a new release-output directory before any SBOM step. Inject:

    github.com/krkarma777/ai-cli-gateway/internal/buildinfo.Version
    github.com/krkarma777/ai-cli-gateway/internal/buildinfo.Commit
    github.com/krkarma777/ai-cli-gateway/internal/buildinfo.Date

Run Syft against the staging directory into a raw file under RUNNER_TEMP. Give only that action step the explicit environment `SYFT_COMPLIANCE_MISSING_NAME=drop`, `SYFT_COMPLIANCE_MISSING_VERSION=stub`, and `SYFT_CHECK_FOR_APP_UPDATE=false`; do not pass a Syft config input or permit ambient compliance settings to choose the main-module representation. Run releasepack sbom with the same validated tag and source epoch to create the canonical collection file in the release output, then run releasepack checksums with the same repository, staging, and output roots.

- [ ] **Step 5: Attest and upload exactly seven assets**

Use one actions/attest provenance call with a newline-delimited subject-path naming:

    ai-cli-gateway_VERSION_linux_amd64.tar.gz
    ai-cli-gateway_VERSION_linux_arm64.tar.gz
    ai-cli-gateway_VERSION_darwin_amd64.tar.gz
    ai-cli-gateway_VERSION_darwin_arm64.tar.gz
    ai-cli-gateway_VERSION_windows_amd64.zip
    ai-cli-gateway_VERSION_sbom.spdx.json
    SHA256SUMS

The actual workflow paths use the validated version output, not a wildcard. Upload those same seven paths as one artifact named release-assets-${{ github.ref_name }} with overwrite false, if-no-files-found error, include-hidden-files false, compression-level 0, and retention-days 1. Export the action’s artifact-id and artifact-digest.

- [ ] **Step 6: Implement read-only verification**

asset-verification downloads only needs.package.outputs.artifact_id to RUNNER_TEMP/release-assets and sets digest-mismatch error. It runs sha256sum --check --strict, then loops over an explicit seven-name shell list.

Each gh attestation verify call includes:

    --repo krkarma777/ai-cli-gateway
    --predicate-type https://slsa.dev/provenance/v1
    --signer-workflow github.com/krkarma777/ai-cli-gateway/.github/workflows/release.yml
    --source-digest ${{ needs.package.outputs.tag_commit }}
    --source-ref refs/tags/${{ github.ref_name }}

Expose GH_TOKEN only as ${{ github.token }} in that fixed step.

- [ ] **Step 7: Implement fail-closed draft-first publication**

publish downloads the same artifact ID with digest mismatch error, rechecks SHA256SUMS, and executes no checked-out code or downloaded executable.

Use one fixed GraphQL query:

    query($owner:String!,$name:String!,$tag:String!){
      repository(owner:$owner,name:$name){
        release(tagName:$tag){id}
      }
    }

Pass owner krkarma777, name ai-cli-gateway, and the validated tag as variables. Capture the full JSON response. With set -e, any gh/network/GraphQL/jq failure exits. A jq gate must require: a top-level object with no errors field; object-valued data; a present object-valued repository; and a present release field whose value is either null or an object containing a nonempty string id. Continue only when release is present and exactly null. A valid release object prints release_already_exists and exits before mutation; every missing field, wrong type, empty id, or other unexpected shape prints release_preflight_invalid and exits. The workflow contract tests explicit-null, existing-object, GraphQL-errors, missing-field, and wrong-type fixtures.

Define a fixed read-only resolver using `gh api` with Accept application/vnd.github+json and X-GitHub-Api-Version 2026-03-10. Read `repos/krkarma777/ai-cli-gateway/git/ref/tags/TAG`, require exact `ref`, object type `commit` or `tag`, and a 40-character lowercase SHA. A commit object is the resolved value. For a tag object, read `repos/krkarma777/ai-cli-gateway/git/tags/TAG_OBJECT_SHA`, require its own sha equals the requested tag-object SHA and its target object type is exactly commit with a valid SHA; reject another tag layer or any malformed/missing field. Require the resolved commit to equal needs.package.outputs.tag_commit immediately before `gh release create`.

Create the draft with all seven explicit paths, --repo krkarma777/ai-cli-gateway, --verify-tag, --draft, --title equal to the tag, and --generate-notes. After upload succeeds, invoke the same live resolver again and require the same package commit immediately before running gh release edit TAG --draft=false. A moved tag leaves the draft non-public and fails closed. Never automatically delete, resume, or overwrite a draft. The security contract evaluates lightweight, one-layer annotated, moved, malformed ref/tag-object, and nested-tag resolver fixtures.

- [ ] **Step 8: Run security GREEN and YAML sanity**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run 'TestReleaseWorkflowContract|TestRepositoryHygiene' -count=1
    ruby -e 'require "yaml"; YAML.load_file(".github/workflows/release.yml", aliases: true)'

Expected: PASS. If the local YAML parser treats on as YAML 1.1 boolean, rely on the Go structural contract for trigger validation and use actionlint when available.

- [ ] **Step 9: Commit**

    git add .github/workflows/release.yml internal/securitytest/repository_test.go
    git commit -m "ci: publish verified tagged releases"

### Task 11: Put a Verified Release Quick Start Before the Contract Reference

**Files:**
- Modify: README.md
- Modify: internal/securitytest/repository_test.go

**Interfaces:**
- Consumes: v0.1.0 asset names, Codex template, curl contract, SDK examples, checksum and attestation commands.
- Produces: separate POSIX and PowerShell five-minute paths without changing the existing detailed API/security reference.

- [ ] **Step 1: Write README onboarding RED assertions**

Add TestREADMEReleaseQuickStart. Preserve the first description paragraph and adjacent subset disclaimer exactly. Require a Quick Start heading before Architecture and scope and require:

- v0.1.0 asset selection for all five tuples;
- POSIX and PowerShell selection of exactly one manifest record for the one downloaded archive, followed by an exact digest comparison;
- tar.gz extraction on POSIX and Expand-Archive for Windows;
- unprivileged binary placement;
- checksum plus optional gh attestation verify with fixed repository/signer/predicate guidance;
- copying examples/config/codex.example.toml;
- four substitutions: executable, config home, runtime root, accessible Codex model;
- chmod 700 on Unix config/runtime homes, chmod 600 on the copied config.toml, and native absolute path guidance on Windows;
- owner-only external gateway-key file generation, format validation, and explicit safe loading in every terminal without a literal secret assignment;
- doctor, serve, authenticated models curl, and response curl;
- Python and JavaScript SDK example commands;
- the exact “Responses API-compatible subset” qualification and short provider-terms notice;
- a systemd source link without placing systemd instructions in the Windows section.

- [ ] **Step 2: Run README RED**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run TestREADMEReleaseQuickStart -count=1

Expected: FAIL because the README begins with Architecture and scope and has no release Quick Start.

- [ ] **Step 3: Add POSIX release and verification commands**

Use fixed v0.1.0 examples and a host-specific asset name. After creating the owner-only configuration directory, the safe key-file pattern is:

    umask 077
    openssl rand -hex 32 > "${GATEWAY_CONFIG_DIR}/gateway.key"
    chmod 600 "${GATEWAY_CONFIG_DIR}/gateway.key"
    GATEWAY_KEY="$(LC_ALL=C tr -d '\n' < "${GATEWAY_CONFIG_DIR}/gateway.key")"
    test "${#GATEWAY_KEY}" -eq 64
    case "${GATEWAY_KEY}" in *[!0-9a-f]*) exit 1 ;; esac
    export AI_CLI_GATEWAY_API_KEY="${GATEWAY_KEY}"

GATEWAY_CONFIG_DIR must be an already validated absolute user-owned directory with mode 0700. Do not assign a literal to AI_CLI_GATEWAY_API_KEY. In the serve terminal and the separate request/SDK terminal, repeat only the bounded read/format-check/export lines from the same absolute key path; never print, source, or execute the key file. The README contract requires both terminal blocks to load it explicitly.

Because the download contains only one archive but SHA256SUMS contains six subject records, use awk with the fixed selected basename to require exactly one matching `*NAME` record, validate its 64 lowercase-hex digest, compute `shasum -a 256` for that archive only, and compare the two values. Do not run `shasum -c` against the incomplete download directory.

Show copying the template to an owner-only configuration directory, chmod 600 on the copied config.toml, replacing the three absolute paths and configured-provider-model, running doctor, running serve in a second terminal, and sending the existing authenticated curl.

For attestations, show gh attestation verify on the selected downloaded archive with:

    --repo krkarma777/ai-cli-gateway
    --predicate-type https://slsa.dev/provenance/v1
    --signer-workflow github.com/krkarma777/ai-cli-gateway/.github/workflows/release.yml

- [ ] **Step 4: Add native PowerShell commands**

Use ai-cli-gateway_0.1.0_windows_amd64.zip, Get-FileHash -Algorithm SHA256, Expand-Archive, a user-owned bin directory, and forward-slash TOML values:

    C:/Tools/Codex/codex.exe
    C:/GatewayService/codex-home
    C:/GatewayService/runtime

State that the user should choose private locations owned by the gateway identity rather than copying those illustrative locations blindly. Read SHA256SUMS, use an array-wrapped exact regex match for the selected archive basename, require Count -eq 1 and a 64-character lowercase-hex digest, compare it case-insensitively with `(Get-FileHash -Algorithm SHA256).Hash`, and fail before extraction on any mismatch.

Generate 32 random bytes with RandomNumberGenerator, lowercase-hex encode them, and write the 64 characters without a BOM or newline to `gateway.key` below the private user-owned config directory. Remove inherited ACLs and grant only the current user read/write with icacls, checking its exit status. Every PowerShell terminal independently reads the file, trims and validates exact `^[0-9a-f]{64}$`, assigns the value to env:AI_CLI_GATEWAY_API_KEY, and never displays or dot-sources it.

- [ ] **Step 5: Add official SDK invocation notes**

Use the SDK sources/manifests/locks bundled in every release archive. Show base URLs including /v1, the configured alias, venv/lock installation, runner-temporary JavaScript install, optional AI_CLI_GATEWAY_TIMEOUT_SECONDS in the 1..300 range (default 300 for real CLI inference), and direct Python/Node example execution. State that these examples validate models.list and one non-streaming responses.create call only; the CI harness overrides the timeout to five seconds for the deterministic fake.

- [ ] **Step 6: Run all README and security contracts**

Run:

    GOCACHE=/private/tmp/spawngate-go-build go test ./internal/securitytest -run 'TestREADME|TestRepositoryHygiene' -count=1
    git diff --check

Expected: PASS without deleting or weakening an existing request, response, error, provider, operations, or security section.

- [ ] **Step 7: Commit**

    git add README.md internal/securitytest/repository_test.go
    git commit -m "docs: add verified release quick start"

### Task 12: Verify, Review, Merge, Protect, Tag, and Audit v0.1.0

**Files:**
- Modify only when a failing verification exposes a defect; return to the owning task’s RED/GREEN cycle before changing code.
- No generated release asset, runtime file, dependency tree, or credential is committed.

**Interfaces:**
- Consumes: every deliverable and test from Tasks 1–11.
- Produces: reviewed main, active release protections, public immutable v0.1.0, seven verified assets, and an evidence-backed completion report.

- [ ] **Step 1: Run the local verification matrix from a clean status**

Run each command separately and retain exit status/output:

    test -z "$(gofmt -l .)"
    go vet ./...
    golangci-lint run ./...
    go test -count=1 ./...
    go test -race -count=1 ./...
    go test -tags=integration -count=1 ./...
    go test -trimpath -count=1 ./...
    GOFLAGS=-trimpath go test -count=1 ./internal/testutil ./internal/testcli
    go test -tags=live -run '^$' ./internal/provider/...
    CGO_ENABLED=0 go build -trimpath -o /private/tmp/ai-cli-gateway ./cmd/ai-cli-gateway
    git diff --check
    git status --short

Expected: every command exits zero and status contains only intentional tracked changes before the final task commit.

- [ ] **Step 2: Run the official SDK black-box contract**

Prepare the Python venv and copied JavaScript workspace below a private external temp root from the exact locks, then run scripts/sdk-contract.sh with their absolute paths. Expected output is exactly the two fixed success markers. After completion, use process inspection and filesystem checks to prove no child or sibling runtime remains.

- [ ] **Step 3: Run a real five-target release dry run**

Create one private external staging root:

    RELEASE_TMP_BASE="${TMPDIR:-/tmp}"
    case "${RELEASE_TMP_BASE}" in /*) ;; *) exit 1 ;; esac
    RELEASE_TMP_BASE="${RELEASE_TMP_BASE%/}"
    test -n "${RELEASE_TMP_BASE}"
    RELEASE_DRY_ROOT="$(mktemp -d "${RELEASE_TMP_BASE}/ai-cli-gateway-release.XXXXXX")"
    chmod 0700 "${RELEASE_DRY_ROOT}"
    mkdir -p "${RELEASE_DRY_ROOT}/bin"

Build the exact five release binaries with v0.1.0 metadata and the normalized current commit timestamp. Build releasepack outside the repository and run archives.

Install Syft v1.50.0 into that private root with:

    GOBIN="${RELEASE_DRY_ROOT}/bin" go install github.com/anchore/syft/cmd/syft@v1.50.0

Run Syft on the five-binary staging directory to raw SPDX JSON under an empty HOME/XDG config and the same explicit `SYFT_COMPLIANCE_MISSING_NAME=drop`, `SYFT_COMPLIANCE_MISSING_VERSION=stub`, and `SYFT_CHECK_FOR_APP_UPDATE=false` environment, then run releasepack sbom and checksums with every required root/tag/time argument. Verify SHA256SUMS, inspect all five archives with the package tests, and run each go version -m check. Remove only the validated private root after evidence is captured.

- [ ] **Step 4: Request independent whole-branch review**

Dispatch a fresh reviewer with the specification, implementation plan, base commit 6afe5bcf6328f8dd85f968b86e648b8e05c3ecc8, and current HEAD. Require severity-ranked findings for correctness, security, API scope, cross-platform behavior, release authority, SDK compatibility, test quality, and documentation. Resolve every Critical or Important finding through a focused semantic RED and fresh verification. Re-review until clean.

- [ ] **Step 5: Commit any review fixes and verify clean history**

Use a scoped commit per reviewed defect. Then run:

    git status --short
    git log --oneline --decorate 6afe5bcf6328f8dd85f968b86e648b8e05c3ecc8..HEAD

Expected: clean worktree and logical task commits including the existing linked-worktree hygiene fix and design/plan commits.

- [ ] **Step 6: Publish the feature branch and merge through GitHub**

Before GitHub mutation, verify:

    gh auth status
    gh api user --jq .login
    gh repo view krkarma777/ai-cli-gateway --json nameWithOwner,isPrivate,defaultBranchRef

Require login krkarma777, public repository, and default branch main. Then:

    git push -u origin feat/v0.1-release-onboarding
    gh pr create --repo krkarma777/ai-cli-gateway --base main --head feat/v0.1-release-onboarding --title "Release v0.1 onboarding" --body-file /private/tmp/ai-cli-gateway-v0.1-pr.md
    gh pr checks --repo krkarma777/ai-cli-gateway --watch
    gh pr merge --repo krkarma777/ai-cli-gateway --merge --delete-branch

The PR body contains a fixed summary, test evidence, security boundary, and no secret/path data. Do not merge while a required check is pending or failing.

- [ ] **Step 7: Enable immutable releases**

Run:

    gh api --method PUT -H 'Accept: application/vnd.github+json' -H 'X-GitHub-Api-Version: 2026-03-10' repos/krkarma777/ai-cli-gateway/immutable-releases
    gh api -H 'Accept: application/vnd.github+json' -H 'X-GitHub-Api-Version: 2026-03-10' repos/krkarma777/ai-cli-gateway/immutable-releases --jq '.enabled == true'

Expected: the PUT succeeds and the GET prints true before any tag is created.

- [ ] **Step 8: Create or verify the v* tag ruleset**

Read existing repository rulesets with Accept: application/vnd.github+json and X-GitHub-Api-Version: 2026-03-10 headers. If no ruleset named Protect v* release tags exists, create an active tag ruleset matching refs/tags/v* with creation, update, and deletion rules and one always-bypass User actor whose ID is the authenticated krkarma777 account. If one exists, require its target, condition, rules, enforcement, and bypass actor to match exactly; never overwrite a different policy.

The creation payload shape is:

    {
      "name": "Protect v* release tags",
      "target": "tag",
      "enforcement": "active",
      "bypass_actors": [
        {"actor_id": AUTHENTICATED_USER_ID, "actor_type": "User", "bypass_mode": "always"}
      ],
      "conditions": {
        "ref_name": {"include": ["refs/tags/v*"], "exclude": []}
      },
      "rules": [
        {"type": "creation"},
        {"type": "update", "parameters": {"update_allows_fetch_and_merge": false}},
        {"type": "deletion"}
      ]
    }

Obtain AUTHENTICATED_USER_ID from gh api user only after requiring login krkarma777. Submit with POST repos/krkarma777/ai-cli-gateway/rulesets using the same two API headers and re-read the returned ruleset with those headers.

- [ ] **Step 9: Tag the merged main commit and monitor the release**

Fetch origin main, require the merged PR commit at origin/main, and create one annotated tag:

    git tag -a v0.1.0 origin/main -m "AI CLI Gateway v0.1.0"
    git push origin refs/tags/v0.1.0

Resolve the unique Release workflow run ID for the pushed tag with gh run list/view, then run gh run watch RUN_ID --exit-status. Do not rerun publication blindly if a draft exists; inspect the draft and use the documented fail-closed recovery.

- [ ] **Step 10: Download and independently verify the public release**

Download all assets into one new private external directory. Require exactly:

    ai-cli-gateway_0.1.0_linux_amd64.tar.gz
    ai-cli-gateway_0.1.0_linux_arm64.tar.gz
    ai-cli-gateway_0.1.0_darwin_amd64.tar.gz
    ai-cli-gateway_0.1.0_darwin_arm64.tar.gz
    ai-cli-gateway_0.1.0_windows_amd64.zip
    ai-cli-gateway_0.1.0_sbom.spdx.json
    SHA256SUMS

Verify all six checksum records. For all seven subjects run gh attestation verify with repository, provenance predicate, canonical release workflow, v0.1.0 source ref, and merged main source digest. Independently resolve and peel the public refs/tags/v0.1.0 through the GitHub REST API with the same strict lightweight/one-layer-annotated rules and require its commit equals the merged main/package digest. Require gh release view v0.1.0 --json isDraft,isPrerelease,tagName,assets,isImmutable to report non-draft, non-prerelease, the exact tag, exactly seven assets, and isImmutable true. Confirm the public README URL renders.

- [ ] **Step 11: Record completion evidence and start the next approved milestone**

Report the merged commit, tag, release URL, seven assets, exact verification commands/results, major design decisions, and every changed file group. Then create a new brainstorming/spec cycle for provider compatibility plus configuration init/Doctor improvements; do not mix that next API/operations work into v0.1.0.
